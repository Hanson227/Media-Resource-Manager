# 影视资源管理器 —— Python 依赖清单
# 安装方式: pip install -r requirements.txt
#
# 打包说明：
#   PyInstaller 打包后约 180-220 MB（含 Python 运行时）
#   使用 opencv-python-headless 节省 ~30 MB（无 GUI 组件）
#   pHash/dHash 纯 numpy 实现，无需 scipy/imagehash（节省 ~130 MB）

# GUI 框架
PySide6>=6.5.0

# 图像处理
Pillow>=10.0.0
# 必须 <5：OpenCV 5 移除了 cv2.dnn.readNetFromCaffe/readNetFromTorch，
# 人脸检测/特征提取会整体失效（hash_engine._load_models 已加 hasattr 兜底）
opencv-python-headless>=4.8.0,<5

# 数值计算
numpy>=1.24.0

# 数据库 ORM
SQLAlchemy>=2.0.0

# HTTP API 服务
fastapi>=0.100.0
uvicorn>=0.22.0

# 文件系统监控
watchdog>=3.0.0

# 系统托盘通知
pystray>=0.19.0
win10toast>=0.9.0

# FFmpeg Python 包装（可选——调用系统 ffmpeg）
ffmpeg-python>=0.2.0

# 回收站操作
send2trash>=1.8.0

# HEIC/HEIF 图片支持
pillow-heif>=0.15.0

# RAW 图片格式支持（可选，~2 MB）
rawpy>=0.21.0
