# 2026-09-04 工作日志

## 系统性代码与设计审查（Media 影视资源管理器）
- 产出：`docs/2026-09-04_code-design-review.md`（结构化清单，P0×8 / P1×8 / P2×7 + 路线图）。
- 关键发现（后续开发务必对照）：
  - P0-真实缺陷：MessageCenter._msg_to_dict 读 `msg.extra_data`（ORM 无此字段）→ 收件箱服务恒空；白名单按钮传 `unit_a_id` 当 dedup_result_id 且从不写 whitelist 表、查重也不读白名单；缩略图 generator 对 cache_dir 再叠 `.thumbnails` 与路由平铺约定冲突（data/.thumbnails/.thumbnails/）；人脸模型目录 `Path("models")` 与置信度 0.6 硬编码，config.face_model_dir 无效。
  - P0-架构：扩展名判定 5 处事实源（config/hash_engine/thumbnail_generator/files.py/media_types）；查重编排 GUI worker 与 /api/dedup/run 双实现（API 漏 face_vectors → 人脸静默失效）；async 路由内跑同步长任务阻塞事件循环；queries 集中原则被路由绕过 + N+1 + 内存分页。
  - 常量/枚举 14 个中 6 个零引用；汉明距离 3 份实现 3 种错误语义；except Exception 158 处。
- 遗留约束：项目约定修改后跑 `python desktop/tests/test_flow.py`，web 改动再跑 `test_web_playwright.py`。

## 依审查报告执行第一批改进（Agent 模式，测试 369/369 通过）
- F08 修复 message_center 僵尸字段：extra_data → action_data（收件箱服务不再恒空）。
- F05 缩略图路径约定统一：ThumbnailGenerator 的 cache_dir 即最终目录（去掉内层 .thumbnails 追加）；文件名改走 constants.THUMBNAIL_FILENAME_TEMPLATE；grid 探测路径改用 generator.get_thumbnail_path；thumb_loader 移除 cache_subdir 参数。消除 data/.thumbnails/.thumbnails 双层与 UI/API 每次请求重复生成。
- F06 人脸参数接入配置：HashEngine 新增 model_dir/face_confidence，hash_worker 从 config 注入；移除硬编码 Path("models")/0.6。
- F01(局部) hash_engine 内联扩展名表删除，改调 utils.media_types（新增 RAW_EXTENSIONS / is_raw_extension / is_perceptual_image_extension）；RAW 仅 MD5，heic 等可尝试感知哈希。
- F18 汉明距离收敛到 utils.hash_helpers（错误语义统一 999）；分块常量改用 HASH_READ_CHUNK_SIZE。
- F17/F22 卫生：migrations 重复编码头、models 12 表 docstring、server/schemas 去除“模拟数据”残留、normalize_path 文档修正、FaceVector 标点。
- 测试基建适配新版 Starlette(fastapi 0.141/starlette 1.6)：test_flow.py 路由自省改递归遍历 original_router；收尾 unlink 兼容沙箱回收站失败（catch OSError）。
- 遗留（下批）：F02 白名单闭环、F03 查重双编排、F04 async 阻塞、F07 分页/N+1、F09 PIN 后端强制、F10 upsert 重置、F11 批量写库、F12/F13/F15/F16/F20/F21。

## 依审查报告执行第二批改进（Agent 模式，测试 369/369 通过）
- F12 配置序列化驱动化：config.py from_file/to_file 改为 dataclasses.fields + 类型注解驱动（Path/frozenset 转换），未知遗留键告警忽略（不再整体回退默认值）；新增 with_updates()（dataclasses.replace）；server.py change-pin 33 字段手抄删除；settings.py _on_save 手抄删除。
- F10/F02 查重处置闭环：upsert_dedup_result 不再重置已处置（is_resolved=True）记录；新增 resolve_dedup_pair/按对处置 + resolution='whitelist' 时同事务写白名单表（unit 级，双方路径）；新增 build_dedup_skip_pairs（已处置对 + 白名单单元 → 重跑跳过，不再重复告警）；DedupEngine.run_dedup 增 skip_pairs 参数；修正 dedup_compare 按钮把 unit_a_id 当 result_id 传的 bug（信号改携带 (unit_a,unit_b)），main_window 改按对处置。
- F21+跨入口互斥：新建 services/dedup_gate.py 进程内互斥门，GUI DedupWorker 与 /api/dedup/run 共用；API 忙碌返回 409。
- F04 路由去 async 阻塞：files/units/dedup/messages/events/tags + server 认证/健康/根端点全部 async def → 同步 def（FastAPI 线程池执行）；auth_verify/change-pin 改 Pydantic body 模型。
- F07 性能：新增 get_files_page（SQL LIMIT/OFFSET 分页）替代全量载入+内存切片；units 列表封面/根名改批量查询去 N+1；删除 /api/units/{id}/files 的静默 200 条截断（此前 SPA/手机端大单元只能看前 200 个文件且 total 不符）；dedup results 列表单元名批量查询。
- 测试：test_dedup_run_api 改用互斥门断言；新增 scenario 验证（白名单闭环/状态保持/skip/分页/配置未知键容忍）。
- 遗留（后续批次）：F03 查重编排双实现合并、F07 events 小项、F09 PIN 后端强制（需前端联动+浏览器测试）、F11 批量写库、F13/F15/F16/F20 等。

## 依审查报告执行第三批改进（Agent 模式，测试 369 → 376/376 通过）
- F03 查重编排收敛：新建 services/dedup_service.py（run_dedup_pipeline：加载(可带人脸向量)→skip_pairs→引擎→落库→提醒，单事务/单实现）；DedupWorker 与 /api/dedup/run 都改为薄壳调用；修正 API 路径此前不加载 face_vectors 的行为分叉。
- F09 Web PIN 后端强制：server.py 进程内令牌（secrets+12h TTL+变更即全量吊销）；配置 web_pin 时 /api/* 经中间件强制 Bearer token（health/auth 端点与 OPTIONS 放行，401 返回 detail）；verify 签发 token、change-pin 吊销全部令牌；web/app.js：api() 按服务器自动附加 Authorization、401 广播 media-auth-expired 回锁屏、verify 成功后存 token、已有有效 token 自动跳过锁屏、断开连接/改密清理。
- F16 哈希索引自愈：scan_worker 发现 md5 为 '' 占位（历史失败/文件曾缺失）时 clear_stale_hash_placeholders 重置 NULL，重新进入待索引队列；避免把永久损坏文件拖入无限重试。
- 测试：新增 test_pin_auth_api（401/verify/伪造 token/带 token 放行）；全套 376/376。
- 遗留（后续）：F11 扫描批量写库（大库吞吐）、F13 枚举收敛、F15 scanner 管线化、F20 web 拆分、README/API.md 文档整体同步（F17 后半）。

## 依审查报告执行第四批改进（Agent 模式，测试 376/376 通过）
- F11 扫描入库批量化：scan_worker._save_to_database 改为一次 IN 查询预载既有文件映射（替代逐文件 get_file_by_path）；新文件收集为 rows_to_add 后统一 add_all+flush；单元归属变更与"空串占位自愈"改为收集后批量执行；失效文件清理改为收集 ID 后单条 bulk delete（依赖 SQLite ON DELETE CASCADE）。已用场景脚本验证正确性与幂等性。
- F13(局部)：schemas.DedupResolveRequest 的 resolution 白名单改为由 ResolutionStatus 枚举生成，消除 Pydantic 正则与枚举双份维护。
- F17 文档同步：README（12 表、端点表补 auth/star/stream/dedup-run/events/tags CRUD、鉴权说明、376 项断言）、CLAUDE.md（12 tables + 查重处置一次性语义说明）、API.md（1.3 鉴权 Bearer 令牌、3.6/3.7 鉴权与事件端点、4.1 缩略图二进制已实现、6 限制表更新）。
- 遗留（后续批次）：F15 scanner 规则管线化与 O(n³)、F13 剩余（models CHECK/查询层状态字符串→枚举收敛）、F20 web 单文件拆分、API.md 章节编号小清理。

## 依审查报告执行第五批改进（Agent 模式，测试 376/376 通过）
- F15 扫描器吞并过滤复杂度收敛：_find_resource_units 用“爬祖先链 + 预计算 desc_count/nearest_parent”替代原两两 relative_to 的 O(n³) 全量重扫（O(n×深度)）；语义等价（原实现注释即“只检查最近的父候选”，现改为确定性最近祖先并跳过已移除祖先）；提升/叶子/容器遍历改 sorted 保证确定性；叶子与容器阈值（2）提为模块常量 MIN_FILES_FOR_LEAF_UNIT / MIN_DIRECT_MEDIA_FOR_CONTAINER。
- F13(追加)：queries.py 高频状态字面量改引用 UnitStatus/ResolutionStatus/WhitelistMatchType 枚举（active/excluded/merged/pending/whitelist/unit），消除查询层与枚举双写。
- 遗留（后续批次）：F20 web 单文件拆分；F13 剩余（models CHECK/前端字面量）；dedup_engine 20x 文件比剪枝魔数；API.md 章节编号小清理。

## 依审查报告执行第六批改进（Agent 模式，测试 376/376 通过）
- dedup 文件比剪枝魔数收敛：DedupEngine 新增构造参数 file_count_ratio_limit（默认 0.05，即 20 倍），替代 compare_units 内联 0.05。
- F13(追加)：db/models.py 7 处 CHECK 约束改由 _enum_in(枚举类) 生成（UnitStatus/MediaType/ResolutionStatus/MatchType/MessageType/WhitelistMatchType/ScanStatus），消除第三份状态清单；枚举顺序与历史 SQL 一致，新旧建库兼容（套件新建库已验证）。
- events 路由收口查询层：新增 q.get_unread_dedup_alert_count()，/api/events/unread 不再直查 Message 模型。
- API.md 章节编号清理：目录补 SMB/移动端集成示例；错误嵌套的 5.1–5.5（首页加载/浏览/查重/网络封装）重编为独立第 6 节 6.1–6.5；原“6. 限制与注意事项”→ 7。
- 至此报告 22 项全部有落地（F13/F15 等大项收尾），剩余仅 F20 web 单文件拆分与零散卫生。

## 收尾批（第七批，测试 376/376 通过）
- 清理未用导入：db/models.py 移除 backref；core/hash_engine.py 移除未使用的 HASH_READ_CHUNK_SIZE 导入。
- 交付物：docs/2026-09-04_code-design-review.md 追加“附录：改进落实情况”对照表（F01–F22 × 状态×落地位置，含测试基建与 376 项说明）；未做项 F19/F20 与部分项余量在表中明确标注。

## 第八批（F04 余量 + F19，测试 376 → 378/378 通过）
- F04 完成：/api/dedup/run 改后台任务（202 + task_id），新增 GET /api/dedup/run/{task_id} 轮询（queued/running/completed/failed + progress/result/error）；任务记录带容量(20)/TTL(1h)清理；互斥门移入后台线程（忙碌则任务 failed 提示）；DedupRunRequest 阈值在提交时快照。test_dedup_run_api 改为轮询断言；README/API.md 同步异步语义。
- F19 部分完成：server.py 注册全局异常处理器（MediaManagerError→500 带原因、HTTPException 透传、Exception 兜底），错误响应全部 JSON；服务层 158 处静默降级逐项分级留待。
- 审查报告附录同步（F04 ✅、F19 ◐、378 项）。

## 第九批（F20 web 拆分完成，后端 378/378 + 浏览器端到端 32/32）
- F20 完成：web/app.js（1564 行）按依赖边界拆为 js-core.js（Vue 解构 + API/令牌工具）、js-pages.js（全部页面组件 + 滚动辅助）、js-app.js（路由 + 根组件 + mount）；index.html 依序引入；sw.js 缓存版本 v2→v3；README web 结构与测试说明、test_web.py 文件级断言同步；旧 app.js 删除。
- 验证基建：media-manager 环境补装 playwright/chromium/requests；test_web_playwright.py 启动加“无系统 Chrome 自动回退 Playwright Chromium”；ensure_db 删除失败（沙箱回收站不可用）时 drop_all 兜底重建；清理重复编码头。
- 浏览器端到端 32/32 通过（页面加载/单元/文件/预览/手势/返回滚动/导航/响应式）。
- 至此报告 F01–F22 全部完成或明确收口；仅 F19 服务层 158 处静默降级分级、F22 main_window 拆分等零散卫生留待后续按需推进。

## 文件夹"内容真实日期"排序修复（测试 388/388 + 浏览器 36/36 通过）
- 需求：文件夹移动后自身 mtime/ctime 变为移动当天，按日期排序错乱。用户确认方案：逻辑字段（不做物理 os.utime）+ 迁移自动回填 + 树文件节点 ctime→mtime。
- 实现：scanner DiscoveredFile 采集 st_mtime + ResourceUnit.latest_mtime property；models.ResourceUnit 新增 content_modified_at 列；migrations v3→v4（列检查 ALTER + _backfill_content_modified_at 按 media_files stat 回填，幂等）；queries create_unit/update_unit_stats 加可选参；scan_worker 三处传值；main_window 手动标记（rglob 取 max mtime）/合并（父子取 max）/树文件节点 st_mtime/卡片 dict；folder_tree._unit_date_str 替换 _get_ctime（content_modified_at → created_at 回退）；FolderCardModel 与 web sortedUnits 排序、卡片显示均改 content_modified_at 优先；API UnitItem/content_modified_at + API.md 文档。
- 测试：test_flow.py 新增 test_content_modified_at（扫描聚合/落库/更新/迁移回填/API 暴露）；test_web_playwright.py 新增 Web 测试 10（ensure_db 两单元置不同 mtime，升/降序断言 + 卡片显示内容日期）。
- 坑：test_db() 在空库阶段就调 migrate_db 把 user_version 升满，迁移测试需先用 PRAGMA user_version=3 降级再走 v3→v4；沙箱回收站不可用导致 test_flow.db unlink 偶发失败（已有 catch，重跑即可）。
- 范围外（后续可选）：web 单元内文件页 sortedFiles 仍按 indexed_at 排序，如需真实时间需给 media_files 加 mtime 列。

## 手机无法访问 Web 端排查（172.20.10.2 拒绝连接）
- 根因：config.json `api_host` 默认 `"127.0.0.1"`（仅回环监听），与 README"默认 0.0.0.0"描述不符；server.py 绑定 config.api_host，LAN IP 上无监听 → 拒绝连接。与排序修改无关（未动 server.py）。
- 处理：config.json api_host → "0.0.0.0"；README 第 37 行修正为真实默认值。需重启应用生效。
- 备注：手机热点网段属公用网络配置文件，若重启后表现为超时而非拒绝连接，则是防火墙拦入站，需放行 TCP 19527（netsh add rule 或弹窗允许）；安全提醒已给出（0.0.0.0 暴露局域网，已有 web_pin 保护）。

## 图片 401 修复（媒体端点 ?token= 鉴权回退，测试 393/393 + 36/36 通过）
- 根因：F09 PIN 强制中间件要求所有 /api/* 带 Bearer 头，但 <img>/<video> 标签请求（thumbnail/stream/poster）无法携带自定义头 → 全部 401。此前未暴露是因为 F09 今天才落地、用户刚重启生效。SW 不处理 /api/，localhost 与 LAN 行为无差异。
- 修复：server.py _extract_bearer_token 回退 request.query_params["token"]；js-core.js 新增 mediaUrl(server, path)（拼 ?token=）；js-pages.js 5 处媒体 URL 改走 mediaUrl（coverUrl/thumbUrl/预览 streamUrl+posterUrl ×2）；API.md 1.3 补"查询参数回退"、第 7 节鉴权行同步。
- 测试：test_pin_auth_api 扩展 5 断言（无令牌 401/伪造 401/?token= 200/头方式仍 200/stream ?token= 206）。

## 移动端视频"下拉退出"手势演示页（独立交付）
- 产出：`web/video-gesture-demo/index.html`（自包含，含 2 个本地示例视频 assets/bigbuckbunny.mp4、assets/flower.mp4，test-videos.co.uk 下载成功、MDN 备用源成功；卡片源缺失自动回退远程源）。
- 核心实现（可复用到正式 web 端）：touchstart/touchmove{passive:false}/touchend/touchcancel + 方向锁定（SLOP=8px，|dy|≥|dx|×1.15 才锁竖直）；阻尼跟手（前 22% 屏高 1:1，超出×0.35，上限 85% 屏高）；速度采样滑动窗口 120ms 算 px/ms；松手判定：offset≥28% vh 或 velocity≥0.55px/ms 且 offset≥60px → 关闭（translate3d(0,110%,0)），否则 settling 类弹簧回弹 cubic-bezier(.22,1,.36,1)；视觉反馈=卡片 translateY+scale(1-6%)+圆角渐显+#backdrop 透明度随进度衰减。
- 兼容要点：video 加 playsinline/webkit-playsinline/x5-playsinline/x5-video-player-type=h5（避免原生全屏层级高于 transform）；player 容器 touch-action:none + body overscroll-behavior-y:contain；.v-bottom 控制条不参与手势；wasDrag 抑制拖动后误触 click 播放暂停；桌面 mouse 事件仅调试用。若后续集成进本项目 web 端，注意图片/视频 URL 已有 ?token= 鉴权回退约定。

## 播放器手势交互审查（5 维度，产出 docs/2026-09-04_gesture-interaction-review.md）
- 审查对象：web/js-pages.js PreviewPage L551-995（单击/双击/长按 2x/横滑快进/暂停横滑切文件/PTR）+ video-gesture-demo。
- P0 发现：①竖直滑动无拒绝（|dy|>|dx| 落入点击分支误切控制条，无方向锁）；②单击与 startHideTimer 竞态——touchend 先 controlsHidden=false、350ms 后 onSingleTap 取反 → 单击净效果=隐藏，控制条无法单击呼出；③iPhone Safari 无元素级 requestFullscreen，toggleFullscreen 失效需 webkitEnterFullscreen 回退；④gesture-zone 无 touch-action:none，iOS 忽略 user-scalable=no 捏合可缩放；⑤多指未处理（changedTouches[0] 跳变 → 快进突跳）。
- P1：切文件纯 40px 距离阈值无速度通道且视觉 clamp=阈值（到顶即切）；快进恒 0.1s/px 无分段无 fling；实时 seek 直打 currentTime（Range 流卡顿，需节流/松手提交）；PTR 无方向/速度检查中途触发；硬 clamp ±40 死墙、切换无滑出滑入；回弹曲线无 spring 且与 demo 不一致；长按倒放 setInterval 步进跳帧；Vue reactive 驱动跟手帧率风险；系统边缘手势冲突。
- 规范建议：抽 web/js-gesture.js 统一状态机（idle→pending→locked{vertical/horizontal/longpress}，identifier 跟踪，多指 abort，touchcancel=中止回弹），token 表（SLOP 10px/轴比 1.15/长按 450ms/双击 300ms/切文件 22%vw 或 0.5px/ms/快进分段 0.1-0.5+fling/阻尼 L0 后×0.35/回弹 320ms cb(.22,1,.36,1)/提交 220ms cb(.4,0,.68,.06)），rAF 直写 transform 绕过 Vue reactive。
- 用户尚未要求落地修改，下一步按 P0→P2 实施。

## 手势代码全面重写（依审查报告落地，web e2e 36/36 通过）
- 新增 `web/js-gesture.js`：统一手势引擎（G_TOKENS Token 表对应报告 §6；createZone 状态机 idle→pending→locked，identifier 跟踪/多指中止/touchcancel=中止/轴比 1.15 锁定/120ms 速度窗口/长按 450ms+250ms 预反馈+激活后超限转滑动；gDamp 阻尼；attachPullToRefresh 助手）；鼠标走同一管线（桌面可手势）。
- js-pages.js：PreviewPage 手势全部改走引擎回调（模板移除内联 touch 绑定与 :style transform，直写 style 绕过 Vue reactive）；①单击立即执行+双击回退控制条（修复 startHideTimer 竞态 P0）②长按左半屏改暂停+rAF 连续倒放③快进 0.1/0.25/0.5 三段系数+150ms 节流提交+fling④切文件双阈值(22%vw∈[72,120]px 或 0.5px/ms)+阻尼跟手+边界阻力/拖中提示+滑出滑入动画+动画期可立即重拖⑤新增下滑退出（v-down 轴：22%vh 后×0.35 阻尼+背景 1→0 透明+scale/圆角，28%vh 或 0.55px/ms 提交→goBack）⑥PTR 迁入 mixin+引擎（松手判定+跟手指示器三态）⑦全屏 iOS webkitEnterFullscreen 回退。模板加 backdrop/sheet 双层。
- style.css：preview-overlay 背景透明+backdrop/sheet 分层、preview-content/gesture-zone touch-action:none+user-select:none、feedback 外层跟随/内层 bubble scale(.12s)、gesture-follow spring .32s+zoomed 类、ptr-indicator.ready、app-main overscroll-behavior。index.html 挂 js-gesture.js；sw.js v3→v4。
- 兼容关键：e2e 用合成 TouchEvent（只 mock changedTouches）在 1280px 视口打 ±120px 横滑 → 引擎保留 touch 事件主路径、changedTouches 回退、NAV_COMMIT_CAP=120px 保证距离路径通过；navigateToFile/路由/props 接口未变。测试 36/36。后续待办：MediaSession、放大态平移、PTR 触发后 indicator 高度由 .active CSS 接管（已验证）。

## 三 bug 修复（手势重写后回归，e2e 36/36 通过）
- ①图片捏合缩放：根因=原先只有双击 1.8x 开关且 .zoomed class 写 transform 与内联冲突。新增 _setZoom/_resetZoom/_toggleImageZoom/_pinch*（js-pages.js），缩放状态统一 _zoom{scale,x,y} 直写 style；双指捏合独立监听（引擎多指中止互补），锚点公式 pan=mid−(mid0−pan0)×s/s0；放大态单指拖动路由 _pan*（替代切文件/下滑退出）；双击倍率 1.8→ZOOM_MAX 2.5；捏合 <1.2 自动复位；CSS .gesture-follow.zoomed 不再写 transform；G_TOKENS 加 ZOOM_MAX/ZOOM_PINCH_MAX/ZOOM_RESET_BELOW。
- ②进度条滑块贴最右端：根因=.progress-fill 缺 position:relative，absolute 滑块锚到 .progress-track。CSS 一行修复。
- ③底部三个时间数字：根因=.progress-seek-hint 无任何 CSS 且无显隐控制（seek 预览气泡常驻显示 seekHintTime 旧值）。模板加 v-if="seeking" + 补气泡 CSS（absolute bottom:16px 居中于 left%）。

## 两交互问题修复（e2e 36/36 通过）
- ①捏合焦点偏向右下角：根因=锚点公式漏掉 (1−k)·L 布局中心项（transform-origin 是元素中心，V(e)=L+t+s·(e−L)，正确解 t1=m1−k(m0−t0)−(1−k)L；旧式把 L 当 0 → 放大时焦点整体漂向右下）。_pinchMove 现用 getBoundingClientRect 中心减当前平移求 L 再代入。经验证：中心捏合 t1=0 不动；角部捏合内容点纹丝不动。
- ②单击打开（进文件夹/开预览）：移动端 iOS10+ 忽略 user-scalable=no → 双击缩放歧义导致 click 延迟/被吞，用户感知为"需要双击"。新增 Gesture.attachFastTap（touchend 直判：位移≤SLOP、时长<LONGPRESS、非多指三重过滤；命中 selector 且不在 exclude 内 preventDefault 抑制合成 click；桌面走 click 分支带 500ms 去重）。UnitsPage unit-card / UnitFilesPage feed-item 移除 @click 改 data-unit-id/data-file-id + 事件委托，card-more 按钮走原生 click 不受影响；CSS 给卡片加 touch-action: manipulation。
